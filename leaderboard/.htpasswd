# LWG Tournament Bot — Dashboard Password File
#
# The hash below uses SHA1 format: {SHA}base64(sha1(password))
# Current password: changeme  ← REPLACE THIS
#
# To generate a new hash for your own password, run:
#   python3 -c "import hashlib,base64; p=input('Pass: '); print('{SHA}'+base64.b64encode(hashlib.sha1(p.encode()).digest()).decode())"
# Then replace the {SHA}... part below with your new hash.
#
# Or use htpasswd on Linux:
#   htpasswd -nbs admin yourpassword
#
admin:{SHA}+pvrmeQCmtWmYVOZ57uuITVghrM=
